# Generated by codegen/generate-language-types.ts from schemas/. Do not edit.
import json
import re
from speechswitch.validation import InputValidator, is_number, is_mapping, is_sequence, is_json_value, utf16_units, code_point_length



def _validate0(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str) and value in ("ar-AE", "ar-EG", "ar-SA", "auto", "bn", "de", "en", "es-ES", "es-MX", "fr", "hi", "id", "it", "ja", "ko", "pt-BR", "pt-PT", "ru", "tr", "vi", "zh",)):
        errors.append(path + ": expected one of \"ar-AE\", \"ar-EG\", \"ar-SA\", \"auto\", \"bn\", \"de\", \"en\", \"es-ES\", \"es-MX\", \"fr\", \"hi\", \"id\", \"it\", \"ja\", \"ko\", \"pt-BR\", \"pt-PT\", \"ru\", \"tr\", \"vi\", \"zh\"")
        return

def _validate1(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str) and value in ("aggressive", "moderate", "none",)):
        errors.append(path + ": expected one of \"aggressive\", \"moderate\", \"none\"")
        return

def _validate2(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str) and value == "grok-tts"):
        errors.append(path + ": expected \"grok-tts\"")
        return

def _validate3(value: object, path: str, errors: list[str]) -> None:
    if not (is_number(value) and value in (32000, 64000, 96000, 128000, 192000,)):
        errors.append(path + ": expected one of 32000, 64000, 96000, 128000, 192000")
        return

def _validate4(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str) and value == "mp3"):
        errors.append(path + ": expected \"mp3\"")
        return

def _validate5(value: object, path: str, errors: list[str]) -> None:
    if not (is_number(value) and value in (8000, 16000, 22050, 24000, 44100, 48000,)):
        errors.append(path + ": expected one of 8000, 16000, 22050, 24000, 44100, 48000")
        return

def _validate6(value: object, path: str, errors: list[str]) -> None:
    if not (is_mapping(value)):
        errors.append(path + ": expected object")
        return
    if "bit_rate_bps" in value:
        _validate3(value["bit_rate_bps"], path + "[\"bitRateBps\"]", errors)
    if "format" in value:
        _validate4(value["format"], path + "[\"format\"]", errors)
    else:
        errors.append(path + "[\"format\"]" + ": required field")
    if "sample_rate_hz" in value:
        _validate5(value["sample_rate_hz"], path + "[\"sampleRateHz\"]", errors)

def _validate7(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str) and value in ("alaw", "mulaw", "pcm", "wav",)):
        errors.append(path + ": expected one of \"alaw\", \"mulaw\", \"pcm\", \"wav\"")
        return

def _validate8(value: object, path: str, errors: list[str]) -> None:
    if not (is_mapping(value)):
        errors.append(path + ": expected object")
        return
    if "format" in value:
        _validate7(value["format"], path + "[\"format\"]", errors)
    else:
        errors.append(path + "[\"format\"]" + ": required field")
    if "sample_rate_hz" in value:
        _validate5(value["sample_rate_hz"], path + "[\"sampleRateHz\"]", errors)
    if "bit_rate_bps" in value:
        errors.append(path + "[\"bitRateBps\"]: field is not allowed")

def _validate9(value: object, path: str, errors: list[str]) -> None:
    start = len(errors)
    before = len(errors)
    _validate6(value, path, errors)
    if len(errors) == before:
        del errors[start:]
        return
    before = len(errors)
    _validate8(value, path, errors)
    if len(errors) == before:
        del errors[start:]
        return

def _validate10(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str)):
        errors.append(path + ": expected string")
        return

def _validate11(value: object, path: str, errors: list[str]) -> None:
    if not (is_mapping(value)):
        errors.append(path + ": expected object")
        return
    if "pattern" in value:
        _validate10(value["pattern"], path + "[\"pattern\"]", errors)
    else:
        errors.append(path + "[\"pattern\"]" + ": required field")
    if "replacement" in value:
        _validate10(value["replacement"], path + "[\"replacement\"]", errors)
    else:
        errors.append(path + "[\"replacement\"]" + ": required field")

def _validate12(value: object, path: str, errors: list[str]) -> None:
    if not (is_sequence(value)):
        errors.append(path + ": expected array")
        return
    for index in range(len(value)):
        _validate11(value[index], path + "[" + str(index) + "]", errors)

def _validate13(value: object, path: str, errors: list[str]) -> None:
    if not (is_number(value)):
        errors.append(path + ": expected finite number")
        return
    if not (is_number(value) and value >= 0.7):
        errors.append(path + ": expected number >= 0.7")
    if not (is_number(value) and value <= 1.5):
        errors.append(path + ": expected number <= 1.5")

def _validate14(value: object, path: str, errors: list[str]) -> None:
    if not (((value is False) or (value is True))):
        errors.append(path + ": expected one of false, true")
        return

def _validate15(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str) and value == "character"):
        errors.append(path + ": expected \"character\"")
        return

def _validate16(value: object, path: str, errors: list[str]) -> None:
    if not (is_mapping(value)):
        errors.append(path + ": expected object")
        return
    if "language" in value:
        _validate0(value["language"], path + "[\"language\"]", errors)
    if "latency_optimization" in value:
        _validate1(value["latency_optimization"], path + "[\"latencyOptimization\"]", errors)
    if "model" in value:
        _validate2(value["model"], path + "[\"model\"]", errors)
    if "output" in value:
        _validate9(value["output"], path + "[\"output\"]", errors)
    if "replacements" in value:
        _validate12(value["replacements"], path + "[\"replacements\"]", errors)
    if "speed" in value:
        _validate13(value["speed"], path + "[\"speed\"]", errors)
    if "text" in value:
        _validate10(value["text"], path + "[\"text\"]", errors)
    else:
        errors.append(path + "[\"text\"]" + ": required field")
    if "text_normalization" in value:
        _validate14(value["text_normalization"], path + "[\"textNormalization\"]", errors)
    if "timestamp_granularity" in value:
        _validate15(value["timestamp_granularity"], path + "[\"timestampGranularity\"]", errors)
    if "voice" in value:
        _validate10(value["voice"], path + "[\"voice\"]", errors)

def _validate17(value: object, path: str, errors: list[str]) -> None:
    if not (callable(getattr(value, "__aiter__", None))):
        errors.append(path + ": expected AsyncIterable")
        return

def _validate18(value: object, path: str, errors: list[str]) -> None:
    if not (is_mapping(value)):
        errors.append(path + ": expected object")
        return
    if "language" in value:
        _validate0(value["language"], path + "[\"language\"]", errors)
    if "latency_optimization" in value:
        _validate1(value["latency_optimization"], path + "[\"latencyOptimization\"]", errors)
    if "model" in value:
        _validate2(value["model"], path + "[\"model\"]", errors)
    if "output" in value:
        _validate9(value["output"], path + "[\"output\"]", errors)
    if "replacements" in value:
        _validate12(value["replacements"], path + "[\"replacements\"]", errors)
    if "speed" in value:
        _validate13(value["speed"], path + "[\"speed\"]", errors)
    if "text" in value:
        _validate17(value["text"], path + "[\"text\"]", errors)
    else:
        errors.append(path + "[\"text\"]" + ": required field")
    if "text_normalization" in value:
        _validate14(value["text_normalization"], path + "[\"textNormalization\"]", errors)
    if "timestamp_granularity" in value:
        _validate15(value["timestamp_granularity"], path + "[\"timestampGranularity\"]", errors)
    if "voice" in value:
        _validate10(value["voice"], path + "[\"voice\"]", errors)

def _validate19(value: object, path: str, errors: list[str]) -> None:
    start = len(errors)
    before = len(errors)
    _validate16(value, path, errors)
    if len(errors) == before:
        del errors[start:]
        return
    before = len(errors)
    _validate18(value, path, errors)
    if len(errors) == before:
        del errors[start:]
        return

def _validate20(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str) and value == "clear"):
        errors.append(path + ": expected \"clear\"")
        return

def _validate21(value: object, path: str, errors: list[str]) -> None:
    if not (is_mapping(value)):
        errors.append(path + ": expected object")
        return
    if "command" in value:
        _validate20(value["command"], path + "[\"command\"]", errors)
    else:
        errors.append(path + "[\"command\"]" + ": required field")

def _validate22(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str) and value == "flush"):
        errors.append(path + ": expected \"flush\"")
        return

def _validate23(value: object, path: str, errors: list[str]) -> None:
    if not (is_mapping(value)):
        errors.append(path + ": expected object")
        return
    if "command" in value:
        _validate22(value["command"], path + "[\"command\"]", errors)
    else:
        errors.append(path + "[\"command\"]" + ": required field")

def _validate24(value: object, path: str, errors: list[str]) -> None:
    if not (isinstance(value, str) and value == "update"):
        errors.append(path + ": expected \"update\"")
        return

def _validate25(value: object, path: str, errors: list[str]) -> None:
    if not (is_mapping(value)):
        errors.append(path + ": expected object")
        return
    if "command" in value:
        _validate24(value["command"], path + "[\"command\"]", errors)
    else:
        errors.append(path + "[\"command\"]" + ": required field")
    if "replacements" in value:
        _validate12(value["replacements"], path + "[\"replacements\"]", errors)
    else:
        errors.append(path + "[\"replacements\"]" + ": required field")

def _validate26(value: object, path: str, errors: list[str]) -> None:
    start = len(errors)
    before = len(errors)
    _validate10(value, path, errors)
    if len(errors) == before:
        del errors[start:]
        return
    before = len(errors)
    _validate21(value, path, errors)
    if len(errors) == before:
        del errors[start:]
        return
    before = len(errors)
    _validate23(value, path, errors)
    if len(errors) == before:
        del errors[start:]
        return
    before = len(errors)
    _validate25(value, path, errors)
    if len(errors) == before:
        del errors[start:]
        return

def validate_request(value: object) -> InputValidator:
    """Validate without advancing input or inserting defaults; paths use canonical schema names."""
    errors: list[str] = []
    _validate19(value, "request", errors)
    if errors:
        raise TypeError("Invalid xai TTS request:\n" + "\n".join(errors))
    _validate18(value, "request", errors)
    accepts0 = not errors
    errors.clear()
    def validate_input(item: object, field: str = "text") -> None:
        errors: list[str] = []
        if accepts0:
            before = len(errors)
            _validate26(item, "text item", errors)
            if len(errors) == before:
                return
        if not errors:
            errors.append("text item: streaming input is not supported by this request")
        raise TypeError("Invalid xai TTS input item:\n" + "\n".join(errors))
    return validate_input
