// Generated by codegen/generate-spec.ts from schemas/providers/async/index.ts. Do not edit.

function validate0(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "string")) { errors.push(path + ": expected string"); return; }
}

function validate1(value: unknown, path: string, errors: string[]): void {
  if (!(value === "de" || value === "en" || value === "es" || value === "fr" || value === "it" || value === "pt")) { errors.push(path + ": expected one of \"de\", \"en\", \"es\", \"fr\", \"it\", \"pt\""); return; }
}

function validate2(value: unknown, path: string, errors: string[]): void {
  if (!(value === "ar" || value === "de" || value === "en" || value === "es" || value === "fr" || value === "he" || value === "hi" || value === "hy" || value === "it" || value === "ja" || value === "pt" || value === "ro" || value === "ru" || value === "tr" || value === "zh")) { errors.push(path + ": expected one of \"ar\", \"de\", \"en\", \"es\", \"fr\", \"he\", \"hi\", \"hy\", \"it\", \"ja\", \"pt\", \"ro\", \"ru\", \"tr\", \"zh\""); return; }
}

function validate3(value: unknown, path: string, errors: string[]): void {
  if (!(value === "flash_v1.5")) { errors.push(path + ": expected \"flash_v1.5\""); return; }
}

function validate4(value: unknown, path: string, errors: string[]): void {
  if (!(value === "castleflow-1.0")) { errors.push(path + ": expected \"castleflow-1.0\""); return; }
}

function validate5(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "number" && Number.isFinite(value))) { errors.push(path + ": expected finite number"); return; }
  if (!(typeof value === "number" && value >= 32000)) { errors.push(path + ": expected number >= 32000"); }
  if (!(Number.isSafeInteger(value))) { errors.push(path + ": expected safe integer"); }
  if (!(typeof value === "number" && value <= 320000)) { errors.push(path + ": expected number <= 320000"); }
}

function validate6(value: unknown, path: string, errors: string[]): void {
  if (!(value === "mp3")) { errors.push(path + ": expected \"mp3\""); return; }
}

function validate7(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "number" && Number.isFinite(value))) { errors.push(path + ": expected finite number"); return; }
  if (!(typeof value === "number" && value >= 8000)) { errors.push(path + ": expected number >= 8000"); }
  if (!(Number.isSafeInteger(value))) { errors.push(path + ": expected safe integer"); }
  if (!(typeof value === "number" && value <= 48000)) { errors.push(path + ": expected number <= 48000"); }
}

function validate8(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("bitRateBps" in value && value["bitRateBps"] !== undefined) validate5(value["bitRateBps"], path + "[\"bitRateBps\"]", errors);
  if ("codec" in value) validate6(value["codec"], path + "[\"codec\"]", errors);
  else errors.push(path + "[\"codec\"]" + ": required field");
  if ("sampleRateHz" in value) validate7(value["sampleRateHz"], path + "[\"sampleRateHz\"]", errors);
  else errors.push(path + "[\"sampleRateHz\"]" + ": required field");
  if ("byteOrder" in value && value["byteOrder"] !== undefined) errors.push(path + "[\"byteOrder\"]: field is not allowed");
  if ("container" in value && value["container"] !== undefined) errors.push(path + "[\"container\"]: field is not allowed");
  if ("sampleFormat" in value && value["sampleFormat"] !== undefined) errors.push(path + "[\"sampleFormat\"]: field is not allowed");
}

function validate9(value: unknown, path: string, errors: string[]): void {
  if (!(value === "mulaw")) { errors.push(path + ": expected \"mulaw\""); return; }
}

function validate10(value: unknown, path: string, errors: string[]): void {
  if (!(value === "raw")) { errors.push(path + ": expected \"raw\""); return; }
}

function validate11(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("codec" in value) validate9(value["codec"], path + "[\"codec\"]", errors);
  else errors.push(path + "[\"codec\"]" + ": required field");
  if ("container" in value) validate10(value["container"], path + "[\"container\"]", errors);
  else errors.push(path + "[\"container\"]" + ": required field");
  if ("sampleRateHz" in value) validate7(value["sampleRateHz"], path + "[\"sampleRateHz\"]", errors);
  else errors.push(path + "[\"sampleRateHz\"]" + ": required field");
  if ("bitRateBps" in value && value["bitRateBps"] !== undefined) errors.push(path + "[\"bitRateBps\"]: field is not allowed");
  if ("byteOrder" in value && value["byteOrder"] !== undefined) errors.push(path + "[\"byteOrder\"]: field is not allowed");
  if ("sampleFormat" in value && value["sampleFormat"] !== undefined) errors.push(path + "[\"sampleFormat\"]: field is not allowed");
}

function validate12(value: unknown, path: string, errors: string[]): void {
  if (!(value === "little_endian")) { errors.push(path + ": expected \"little_endian\""); return; }
}

function validate13(value: unknown, path: string, errors: string[]): void {
  if (!(value === "pcm")) { errors.push(path + ": expected \"pcm\""); return; }
}

function validate14(value: unknown, path: string, errors: string[]): void {
  if (!(value === "float32" || value === "int16")) { errors.push(path + ": expected one of \"float32\", \"int16\""); return; }
}

function validate15(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("byteOrder" in value && value["byteOrder"] !== undefined) validate12(value["byteOrder"], path + "[\"byteOrder\"]", errors);
  if ("codec" in value) validate13(value["codec"], path + "[\"codec\"]", errors);
  else errors.push(path + "[\"codec\"]" + ": required field");
  if ("container" in value) validate10(value["container"], path + "[\"container\"]", errors);
  else errors.push(path + "[\"container\"]" + ": required field");
  if ("sampleFormat" in value && value["sampleFormat"] !== undefined) validate14(value["sampleFormat"], path + "[\"sampleFormat\"]", errors);
  if ("sampleRateHz" in value) validate7(value["sampleRateHz"], path + "[\"sampleRateHz\"]", errors);
  else errors.push(path + "[\"sampleRateHz\"]" + ": required field");
  if ("bitRateBps" in value && value["bitRateBps"] !== undefined) errors.push(path + "[\"bitRateBps\"]: field is not allowed");
}

function validate16(value: unknown, path: string, errors: string[]): void {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    errors.push(path + ": expected object");
    return;
  }
  switch ("codec" in value ? value["codec"] : undefined) {
    case "mp3": {
      validate8(value, path, errors);
      return;
    }
    case "mulaw": {
      validate11(value, path, errors);
      return;
    }
    case "pcm": {
      validate15(value, path, errors);
      return;
    }
    default: errors.push(path + "[\"codec\"]: expected one of \"mp3\", \"mulaw\", \"pcm\"");
  }
}

function validate17(value: unknown, path: string, errors: string[]): void {
  if (!(value === "wav")) { errors.push(path + ": expected \"wav\""); return; }
}

function validate18(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("byteOrder" in value && value["byteOrder"] !== undefined) validate12(value["byteOrder"], path + "[\"byteOrder\"]", errors);
  if ("codec" in value) validate13(value["codec"], path + "[\"codec\"]", errors);
  else errors.push(path + "[\"codec\"]" + ": required field");
  if ("container" in value) validate17(value["container"], path + "[\"container\"]", errors);
  else errors.push(path + "[\"container\"]" + ": required field");
  if ("sampleFormat" in value && value["sampleFormat"] !== undefined) validate14(value["sampleFormat"], path + "[\"sampleFormat\"]", errors);
  if ("sampleRateHz" in value) validate7(value["sampleRateHz"], path + "[\"sampleRateHz\"]", errors);
  else errors.push(path + "[\"sampleRateHz\"]" + ": required field");
  if ("bitRateBps" in value && value["bitRateBps"] !== undefined) errors.push(path + "[\"bitRateBps\"]: field is not allowed");
}

function validate19(value: unknown, path: string, errors: string[]): void {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    errors.push(path + ": expected object");
    return;
  }
  switch ("codec" in value ? value["codec"] : undefined) {
    case "mp3": {
      validate8(value, path, errors);
      return;
    }
    case "mulaw": {
      validate11(value, path, errors);
      return;
    }
    case "pcm": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("container" in value ? value["container"] : undefined) {
        case "raw": {
          validate15(value, path, errors);
          return;
        }
        case "wav": {
          validate18(value, path, errors);
          return;
        }
        default: errors.push(path + "[\"container\"]: expected one of \"raw\", \"wav\"");
      }
      return;
    }
    default: errors.push(path + "[\"codec\"]: expected one of \"mp3\", \"mulaw\", \"pcm\"");
  }
}

function validate20(value: unknown, path: string, errors: string[]): void {
  if (!(value === "immediate" || value === "sentence")) { errors.push(path + ": expected one of \"immediate\", \"sentence\""); return; }
}

function validate21(value: unknown, path: string, errors: string[]): void {
  if (!((typeof value === "object" || typeof value === "function") && value !== null && Symbol.asyncIterator in value && typeof value[Symbol.asyncIterator] === "function")) { errors.push(path + ": expected AsyncIterable"); return; }
}

function validate22(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate1(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate3(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate16(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) validate20(value["segmentation"], path + "[\"segmentation\"]", errors);
  if ("text" in value) validate21(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("speed" in value && value["speed"] !== undefined) errors.push(path + "[\"speed\"]: field is not allowed");
  if ("stability" in value && value["stability"] !== undefined) errors.push(path + "[\"stability\"]: field is not allowed");
  if ("timestampGranularity" in value && value["timestampGranularity"] !== undefined) errors.push(path + "[\"timestampGranularity\"]: field is not allowed");
}

function validate23(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "number" && Number.isFinite(value))) { errors.push(path + ": expected finite number"); return; }
  if (!(typeof value === "number" && value >= 0.7)) { errors.push(path + ": expected number >= 0.7"); }
  if (!(typeof value === "number" && value <= 2)) { errors.push(path + ": expected number <= 2"); }
}

function validate24(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "number" && Number.isFinite(value))) { errors.push(path + ": expected finite number"); return; }
  if (!(typeof value === "number" && value >= 0)) { errors.push(path + ": expected number >= 0"); }
  if (!(typeof value === "number" && value <= 1)) { errors.push(path + ": expected number <= 1"); }
}

function validate25(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate2(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate4(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate16(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) validate20(value["segmentation"], path + "[\"segmentation\"]", errors);
  if ("speed" in value && value["speed"] !== undefined) validate23(value["speed"], path + "[\"speed\"]", errors);
  if ("stability" in value && value["stability"] !== undefined) validate24(value["stability"], path + "[\"stability\"]", errors);
  if ("text" in value) validate21(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("timestampGranularity" in value && value["timestampGranularity"] !== undefined) errors.push(path + "[\"timestampGranularity\"]: field is not allowed");
}

function validate26(value: unknown, path: string, errors: string[]): void {
  if (!(value === "en")) { errors.push(path + ": expected \"en\""); return; }
}

function validate27(value: unknown, path: string, errors: string[]): void {
  if (!(value === "pro_v1.0")) { errors.push(path + ": expected \"pro_v1.0\""); return; }
}

function validate28(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate26(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate27(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate16(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) validate20(value["segmentation"], path + "[\"segmentation\"]", errors);
  if ("text" in value) validate21(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("speed" in value && value["speed"] !== undefined) errors.push(path + "[\"speed\"]: field is not allowed");
  if ("stability" in value && value["stability"] !== undefined) errors.push(path + "[\"stability\"]: field is not allowed");
  if ("timestampGranularity" in value && value["timestampGranularity"] !== undefined) errors.push(path + "[\"timestampGranularity\"]: field is not allowed");
}

function validate29(value: unknown, path: string, errors: string[]): void {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    errors.push(path + ": expected object");
    return;
  }
  switch ("language" in value ? value["language"] : undefined) {
    case "de": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          validate22(value, path, errors);
          return;
        }
        case "castleflow-1.0": {
          validate25(value, path, errors);
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case "en": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          validate22(value, path, errors);
          return;
        }
        case "castleflow-1.0": {
          validate25(value, path, errors);
          return;
        }
        case "pro_v1.0": {
          validate28(value, path, errors);
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\", \"pro_v1.0\"");
      }
      return;
    }
    case "es": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          validate22(value, path, errors);
          return;
        }
        case "castleflow-1.0": {
          validate25(value, path, errors);
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case "fr": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          validate22(value, path, errors);
          return;
        }
        case "castleflow-1.0": {
          validate25(value, path, errors);
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case "it": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          validate22(value, path, errors);
          return;
        }
        case "castleflow-1.0": {
          validate25(value, path, errors);
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case "pt": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          validate22(value, path, errors);
          return;
        }
        case "castleflow-1.0": {
          validate25(value, path, errors);
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case undefined: {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          validate22(value, path, errors);
          return;
        }
        case "castleflow-1.0": {
          validate25(value, path, errors);
          return;
        }
        case "pro_v1.0": {
          validate28(value, path, errors);
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\", \"pro_v1.0\"");
      }
      return;
    }
    case "ar": {
      validate25(value, path, errors);
      return;
    }
    case "he": {
      validate25(value, path, errors);
      return;
    }
    case "hi": {
      validate25(value, path, errors);
      return;
    }
    case "hy": {
      validate25(value, path, errors);
      return;
    }
    case "ja": {
      validate25(value, path, errors);
      return;
    }
    case "ro": {
      validate25(value, path, errors);
      return;
    }
    case "ru": {
      validate25(value, path, errors);
      return;
    }
    case "tr": {
      validate25(value, path, errors);
      return;
    }
    case "zh": {
      validate25(value, path, errors);
      return;
    }
    default: errors.push(path + "[\"language\"]: expected one of \"de\", \"en\", \"es\", \"fr\", \"it\", \"pt\", undefined, \"ar\", \"he\", \"hi\", \"hy\", \"ja\", \"ro\", \"ru\", \"tr\", \"zh\"");
  }
}

function validate30(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate1(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate3(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate16(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) validate20(value["segmentation"], path + "[\"segmentation\"]", errors);
  if ("text" in value) validate21(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("voice" in value) validate0(value["voice"], path + "[\"voice\"]", errors);
  else errors.push(path + "[\"voice\"]" + ": required field");
  if ("speed" in value && value["speed"] !== undefined) errors.push(path + "[\"speed\"]: field is not allowed");
  if ("stability" in value && value["stability"] !== undefined) errors.push(path + "[\"stability\"]: field is not allowed");
  if ("timestampGranularity" in value && value["timestampGranularity"] !== undefined) errors.push(path + "[\"timestampGranularity\"]: field is not allowed");
}

function validate31(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate1(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate3(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate19(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("text" in value) validate0(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("voice" in value) validate0(value["voice"], path + "[\"voice\"]", errors);
  else errors.push(path + "[\"voice\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) errors.push(path + "[\"segmentation\"]: field is not allowed");
  if ("speed" in value && value["speed"] !== undefined) errors.push(path + "[\"speed\"]: field is not allowed");
  if ("stability" in value && value["stability"] !== undefined) errors.push(path + "[\"stability\"]: field is not allowed");
  if ("timestampGranularity" in value && value["timestampGranularity"] !== undefined) errors.push(path + "[\"timestampGranularity\"]: field is not allowed");
}

function validate32(value: unknown, path: string, errors: string[]): void {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    errors.push(path + ": expected object");
    return;
  }
  switch ("codec" in value ? value["codec"] : undefined) {
    case "mp3": {
      validate8(value, path, errors);
      return;
    }
    case "pcm": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("container" in value ? value["container"] : undefined) {
        case "raw": {
          validate15(value, path, errors);
          return;
        }
        case "wav": {
          validate18(value, path, errors);
          return;
        }
        default: errors.push(path + "[\"container\"]: expected one of \"raw\", \"wav\"");
      }
      return;
    }
    default: errors.push(path + "[\"codec\"]: expected one of \"mp3\", \"pcm\"");
  }
}

function validate33(value: unknown, path: string, errors: string[]): void {
  if (!(value === "word")) { errors.push(path + ": expected \"word\""); return; }
}

function validate34(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate1(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate3(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate32(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("text" in value) validate0(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("timestampGranularity" in value) validate33(value["timestampGranularity"], path + "[\"timestampGranularity\"]", errors);
  else errors.push(path + "[\"timestampGranularity\"]" + ": required field");
  if ("voice" in value) validate0(value["voice"], path + "[\"voice\"]", errors);
  else errors.push(path + "[\"voice\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) errors.push(path + "[\"segmentation\"]: field is not allowed");
  if ("speed" in value && value["speed"] !== undefined) errors.push(path + "[\"speed\"]: field is not allowed");
  if ("stability" in value && value["stability"] !== undefined) errors.push(path + "[\"stability\"]: field is not allowed");
}

function validate35(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate2(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate4(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate16(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) validate20(value["segmentation"], path + "[\"segmentation\"]", errors);
  if ("speed" in value && value["speed"] !== undefined) validate23(value["speed"], path + "[\"speed\"]", errors);
  if ("stability" in value && value["stability"] !== undefined) validate24(value["stability"], path + "[\"stability\"]", errors);
  if ("text" in value) validate21(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("voice" in value) validate0(value["voice"], path + "[\"voice\"]", errors);
  else errors.push(path + "[\"voice\"]" + ": required field");
  if ("timestampGranularity" in value && value["timestampGranularity"] !== undefined) errors.push(path + "[\"timestampGranularity\"]: field is not allowed");
}

function validate36(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate2(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate4(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate19(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("speed" in value && value["speed"] !== undefined) validate23(value["speed"], path + "[\"speed\"]", errors);
  if ("stability" in value && value["stability"] !== undefined) validate24(value["stability"], path + "[\"stability\"]", errors);
  if ("text" in value) validate0(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("voice" in value) validate0(value["voice"], path + "[\"voice\"]", errors);
  else errors.push(path + "[\"voice\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) errors.push(path + "[\"segmentation\"]: field is not allowed");
  if ("timestampGranularity" in value && value["timestampGranularity"] !== undefined) errors.push(path + "[\"timestampGranularity\"]: field is not allowed");
}

function validate37(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate2(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate4(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate32(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("speed" in value && value["speed"] !== undefined) validate23(value["speed"], path + "[\"speed\"]", errors);
  if ("stability" in value && value["stability"] !== undefined) validate24(value["stability"], path + "[\"stability\"]", errors);
  if ("text" in value) validate0(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("timestampGranularity" in value) validate33(value["timestampGranularity"], path + "[\"timestampGranularity\"]", errors);
  else errors.push(path + "[\"timestampGranularity\"]" + ": required field");
  if ("voice" in value) validate0(value["voice"], path + "[\"voice\"]", errors);
  else errors.push(path + "[\"voice\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) errors.push(path + "[\"segmentation\"]: field is not allowed");
}

function validate38(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate26(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate27(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate16(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) validate20(value["segmentation"], path + "[\"segmentation\"]", errors);
  if ("text" in value) validate21(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("voice" in value) validate0(value["voice"], path + "[\"voice\"]", errors);
  else errors.push(path + "[\"voice\"]" + ": required field");
  if ("speed" in value && value["speed"] !== undefined) errors.push(path + "[\"speed\"]: field is not allowed");
  if ("stability" in value && value["stability"] !== undefined) errors.push(path + "[\"stability\"]: field is not allowed");
  if ("timestampGranularity" in value && value["timestampGranularity"] !== undefined) errors.push(path + "[\"timestampGranularity\"]: field is not allowed");
}

function validate39(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate26(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate27(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate19(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("text" in value) validate0(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("voice" in value) validate0(value["voice"], path + "[\"voice\"]", errors);
  else errors.push(path + "[\"voice\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) errors.push(path + "[\"segmentation\"]: field is not allowed");
  if ("speed" in value && value["speed"] !== undefined) errors.push(path + "[\"speed\"]: field is not allowed");
  if ("stability" in value && value["stability"] !== undefined) errors.push(path + "[\"stability\"]: field is not allowed");
  if ("timestampGranularity" in value && value["timestampGranularity"] !== undefined) errors.push(path + "[\"timestampGranularity\"]: field is not allowed");
}

function validate40(value: unknown, path: string, errors: string[]): void {
  if (!(typeof value === "object" && value !== null && !Array.isArray(value))) { errors.push(path + ": expected object"); return; }
  if ("language" in value && value["language"] !== undefined) validate26(value["language"], path + "[\"language\"]", errors);
  if ("model" in value) validate27(value["model"], path + "[\"model\"]", errors);
  else errors.push(path + "[\"model\"]" + ": required field");
  if ("output" in value) validate32(value["output"], path + "[\"output\"]", errors);
  else errors.push(path + "[\"output\"]" + ": required field");
  if ("text" in value) validate0(value["text"], path + "[\"text\"]", errors);
  else errors.push(path + "[\"text\"]" + ": required field");
  if ("timestampGranularity" in value) validate33(value["timestampGranularity"], path + "[\"timestampGranularity\"]", errors);
  else errors.push(path + "[\"timestampGranularity\"]" + ": required field");
  if ("voice" in value) validate0(value["voice"], path + "[\"voice\"]", errors);
  else errors.push(path + "[\"voice\"]" + ": required field");
  if ("segmentation" in value && value["segmentation"] !== undefined) errors.push(path + "[\"segmentation\"]: field is not allowed");
  if ("speed" in value && value["speed"] !== undefined) errors.push(path + "[\"speed\"]: field is not allowed");
  if ("stability" in value && value["stability"] !== undefined) errors.push(path + "[\"stability\"]: field is not allowed");
}

function validate41(value: unknown, path: string, errors: string[]): void {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    errors.push(path + ": expected object");
    return;
  }
  switch ("language" in value ? value["language"] : undefined) {
    case "de": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate30(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate31(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate34(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        case "castleflow-1.0": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate35(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate36(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate37(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case "en": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate30(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate31(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate34(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        case "castleflow-1.0": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate35(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate36(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate37(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        case "pro_v1.0": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate38(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate39(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate40(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\", \"pro_v1.0\"");
      }
      return;
    }
    case "es": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate30(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate31(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate34(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        case "castleflow-1.0": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate35(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate36(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate37(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case "fr": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate30(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate31(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate34(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        case "castleflow-1.0": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate35(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate36(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate37(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case "it": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate30(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate31(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate34(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        case "castleflow-1.0": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate35(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate36(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate37(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case "pt": {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate30(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate31(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate34(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        case "castleflow-1.0": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate35(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate36(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate37(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\"");
      }
      return;
    }
    case undefined: {
      if (typeof value !== "object" || value === null || Array.isArray(value)) {
        errors.push(path + ": expected object");
        return;
      }
      switch ("model" in value ? value["model"] : undefined) {
        case "flash_v1.5": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate30(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate31(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate34(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        case "castleflow-1.0": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate35(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate36(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate37(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        case "pro_v1.0": {
          const start = errors.length;
          let before: number;
          before = errors.length;
          validate38(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate39(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          before = errors.length;
          validate40(value, path, errors);
          if (errors.length === before) { errors.length = start; return; }
          return;
        }
        default: errors.push(path + "[\"model\"]: expected one of \"flash_v1.5\", \"castleflow-1.0\", \"pro_v1.0\"");
      }
      return;
    }
    case "ar": {
      const start = errors.length;
      let before: number;
      before = errors.length;
      validate35(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate36(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate37(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      return;
    }
    case "he": {
      const start = errors.length;
      let before: number;
      before = errors.length;
      validate35(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate36(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate37(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      return;
    }
    case "hi": {
      const start = errors.length;
      let before: number;
      before = errors.length;
      validate35(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate36(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate37(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      return;
    }
    case "hy": {
      const start = errors.length;
      let before: number;
      before = errors.length;
      validate35(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate36(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate37(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      return;
    }
    case "ja": {
      const start = errors.length;
      let before: number;
      before = errors.length;
      validate35(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate36(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate37(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      return;
    }
    case "ro": {
      const start = errors.length;
      let before: number;
      before = errors.length;
      validate35(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate36(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate37(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      return;
    }
    case "ru": {
      const start = errors.length;
      let before: number;
      before = errors.length;
      validate35(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate36(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate37(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      return;
    }
    case "tr": {
      const start = errors.length;
      let before: number;
      before = errors.length;
      validate35(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate36(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate37(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      return;
    }
    case "zh": {
      const start = errors.length;
      let before: number;
      before = errors.length;
      validate35(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate36(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      before = errors.length;
      validate37(value, path, errors);
      if (errors.length === before) { errors.length = start; return; }
      return;
    }
    default: errors.push(path + "[\"language\"]: expected one of \"de\", \"en\", \"es\", \"fr\", \"it\", \"pt\", undefined, \"ar\", \"he\", \"hi\", \"hy\", \"ja\", \"ro\", \"ru\", \"tr\", \"zh\"");
  }
}

/** Validate the request without consuming or changing its input. */
export function validateRequest(value: unknown): void {
  const errors: string[] = [];
  validate41(value, "request", errors);
  if (errors.length) throw new TypeError("Invalid async TTS request" + ":\n" + errors.join("\n"));
}

/** Validate an item from an already validated request; never consumes or changes the request. */
export function validateInputItem(value: unknown, item: unknown): void {
  const errors: string[] = [];
  {
    const before = errors.length;
    validate29(value, "request", errors);
    const matches = errors.length === before;
    errors.length = before;
    if (matches) {
      validate0(item, "text item", errors);
      if (errors.length === before) return;
    }
  }
  if (!errors.length) errors.push("text item: streaming input is not supported by this request");
  throw new TypeError("Invalid async TTS input item" + ":\n" + errors.join("\n"));
}
